<a class="btn btn-primary" href="/admin/brand/create">create my brand</a>
<div id ="alert-close-1s-tablebrand"class="alert alert-info alert-dismissible fade show">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
   <span id="info-alert-deletep-password"><strong>Info!</strong> delete brand seeced.</span> 
  </div>
<table class="table table-hover table-sm table-bordered">
  <thead class="text-center">
    <tr>
      <th>index</th>
      <th>name brand</th>
      <th>created at</th>
      <th>updated at</th>
      <th>amount product</th>
      <th>feature</th>
    </tr>
  </thead>
  <tbody class="text-center">
    <% let indexBrand=1 %>
      <% for(let i=0; i <listBrands.length; i++){ %>
  
        <tr>
          <td>
            <%= indexBrand++ %>
          </td>
          <td>
            <%= listBrands[i].name %>
          </td>
          <td>
            <%= listBrands[i].createdAt %>
          </td>
          <td>
            <%= listBrands[i].updatedAt %>
          </td>
          <td>
            <%= listBrands[i].name %>
          </td>
          <td>
            <button class="btn btn-danger btn-sm btn-delete-brand" data-id-brand="<%= listBrands[i]._id %>">Delete</button>
            <a href="/admin/brand/update/id/<%= listBrands[i]._id %>" class="btn btn-warning btn-sm">update</a>
          </td>
        </tr>
        <% } %>  
  </tbody>
</table>